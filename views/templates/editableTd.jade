td(id='#{id}').controls.dont-export
  i.close.fa.fa-fw.fa-times-circle

  style.
    table.#{tableId} td.squeezedElement,
    table.#{tableId} th.squeezedElement {
      width: #{width - 24 - 8 - 8 - 1}px !important;
    }
